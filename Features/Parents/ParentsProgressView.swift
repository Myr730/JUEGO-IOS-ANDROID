//
//  ParentsProgressView.swift
//  Aventuras con Nutri
//
//  Created by Victoria García on 21/09/25.
//

import SwiftUI
struct ParentsProgressView: View {
    var body: some View {
        AppTheme {
            Text("PROGRESO (maqueta)").font(TypeScale.titleM)
                .frame(maxWidth: .infinity, maxHeight: .infinity)
                .background(Color(hex:"#EAF3FB").ignoresSafeArea())
        }
    }
}
