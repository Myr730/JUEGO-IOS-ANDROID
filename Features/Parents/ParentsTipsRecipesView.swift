//
//  ParentsTipsRecipesView.swift
//  Aventuras con Nutri
//
//  Created by Victoria García on 21/09/25.
//

import SwiftUI
struct ParentsTipsRecipesView: View {
    var body: some View {
        AppTheme {
            Text("CONSEJOS Y RECETAS (maqueta)").font(TypeScale.titleM)
                .frame(maxWidth: .infinity, maxHeight: .infinity)
                .background(Color(hex:"#ECF8F6").ignoresSafeArea())
        }
    }
}
